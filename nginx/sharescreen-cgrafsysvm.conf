# Trecho ShareScreen — já integrado em nginx.conf.cgrafsysvm-completo.conf
# Use o arquivo completo ou copie apenas o bloco "SISTEMA 4" para o seu nginx.conf
#
# Pré-requisito: start-producao.bat em C:\Sistemas CGraf\Screen Share
# WebRTC: UDP 40000-40100 no IP do servidor (não passa pelo Nginx)
